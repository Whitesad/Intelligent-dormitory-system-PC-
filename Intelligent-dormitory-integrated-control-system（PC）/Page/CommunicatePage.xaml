<Page
    x:Class="Intelligent_dormitory_integrated_control_system_PC_.CommunicatePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Intelligent_dormitory_integrated_control_system_PC_"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto">
            </RowDefinition>
            <RowDefinition Height="*">
            </RowDefinition>
        </Grid.RowDefinitions>
        <!--第一行-->
        <WebView Grid.Row="0"
                 Name="ChatBox" HorizontalAlignment="Stretch" Margin="0,0,0,243.4" VerticalAlignment="Stretch">
        </WebView>
        <!--第二行-->
        <Grid Margin="0,300,0,0" VerticalAlignment="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*">
                </ColumnDefinition>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="1" Padding="10" VerticalAlignment="Bottom">
                <TextBox Name="TextSend"
                         Margin="4.6,0,5.4,0" Height="114" HorizontalAlignment="Stretch"/>
                <Button Content="发送！"
                        FontSize="40"
                        HorizontalAlignment="Left"
                        Click="SendButton_Click" Height="109" VerticalAlignment="Center" Width="168"/>
            </StackPanel>
        </Grid>

    </Grid>
</Page>
